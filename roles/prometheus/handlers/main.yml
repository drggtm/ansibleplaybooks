---
- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: yes

- name: Restart prometheus
  service:
    name: prometheus
    state: restarted

- name: Reload prometheus
  service:
    name: prometheus
    state: reloaded
